import { TipoNativoSimbolo } from "@designliquido/delegua/inferenciador";
export type TipoInferencia = 'cadeia' | 'caracter' | 'inteiro' | 'lógico' | 'real' | 'vazio';
export declare function inferirTipoVariavel(variavel: string | number | Array<any> | boolean | null | undefined): TipoInferencia | TipoNativoSimbolo;
/**
 * Conversão de valores de variáveis.
 * @param {string} tipo Possíveis valores: "inteiro", "lógico", "real", "módulo", ou ainda
 *                      "inteiro[]", "lógico[]", "real[]".
 * @param valor Normalmente um vetor (_array_) se o tipo termina com "[]".
 *              Caso contrário, o valor é um tipo primitivo que pode ser convertido.
 * @returns O valor convertido, de acordo com o tipo da variável.
 */
export declare function converterValor(tipo: string, valor: any): any;
//# sourceMappingURL=inferenciador.d.ts.map